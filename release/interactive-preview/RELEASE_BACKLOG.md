# Shared Blueprint Interactive Preview release backlog

- Host a real public demo before adding a Try Demo URL.
- Upstream or otherwise publish the generic DSH extension APIs so the standalone bundle no longer needs a compatibility checkout.
- Reserve and confirm registry ownership of `dsh-shared-blueprint` before any npm publication.
- Revisit capability-authoring reasoning presentation as a P2 UX item; this release intentionally keeps the accepted user-level configuring state and does not change presentation or lifecycle code.
- Add Inspect Mode only after separate product acceptance and release packaging.
